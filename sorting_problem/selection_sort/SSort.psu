SELECTION-SORT(A, n)
// loop invariant: elements in the sub-array to the left of current element are sorted in their final order 

for i = 1 to n - 1
    for j = i + 1 to n
        if A[i] > A[j]
            tmp_value = A[i]
            A[i] = A[j]
            A[j] = tmp_value